= simple_form_for [:admin, @event, @guest] do |f|
  = f.input :name
  = f.input :invited_to_ceremony
  = f.input :invited_to_evening
  = f.association :group, label_method: :title
  = f.button :submit
